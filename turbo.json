{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"build": {
			"outputs": ["dist/**"]
		},
		"lint": {
			"outputMode": "new-only"
		},
		"typecheck": {
			"outputMode": "new-only"
		},
		"dev": {
			"cache": false,
			"persistent": true
		}
	}
}
